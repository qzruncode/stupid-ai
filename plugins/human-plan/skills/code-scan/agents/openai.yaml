interface:
  display_name: "Code Scan"
  short_description: "Scan code risks, then auto-loop the top plan to the gate."
  default_prompt: "/human-plan:code-scan 扫描当前项目代码，找出最需要修的问题并自动推进到 approve gate"
